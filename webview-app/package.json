{
    "name": "opentrace-webview",
    "version": "1.0.0",
    "private": true,
    "description": "OpenTrace webview application",
    "scripts": {
        "build": "stencil build",
        "dev": "stencil build --dev --watch",
        "generate": "stencil generate"
    },
    "dependencies": {
        "@stencil/core": "^2.22.0",
        "pixi.js": "^5.3.0"
    },
    "devDependencies": {
        "typescript": "~4.9.0"
    }
}
